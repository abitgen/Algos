pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = 'algos'
rootProject.buildFileName = 'build.gradle'
include(":kotlin")

